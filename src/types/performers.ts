export type Performer = {
    id: string;
    order: string;
    name: string;
    club: string;
    category_id: string;
    routine: string;
}

export type Category = {
    id?: string;
    name: string;
}

